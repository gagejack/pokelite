// The build's version — the one place it is written down.
//
// Bump this when you ship. Everything that displays a version reads it from
// here (the menu's version tag, the update notice's band), so the number can
// never disagree with itself across the UI.
//
// Shipping a feature is two edits: this, and the notice in game/updates.js.
export const VERSION = 'v1.4'
